name: Verify Installers

on:
  workflow_dispatch:
  push:
    branches: [ "main" ]
    paths:
      - 'src/assets/setup/**'
      - '.github/workflows/verify-installers.yml'
  pull_request:
    paths:
      - 'src/assets/setup/**'
      - '.github/workflows/verify-installers.yml'

jobs:
  verify-install:
    runs-on: ubuntu-latest
    container:
      image: ubuntu:24.04
    strategy:
      matrix:
        component: [swift, uv, just, gh]
      fail-fast: false

    steps:
      - name: Install base packages
        run: |
          apt-get update
          apt-get install -y --no-install-recommends \
            bash ca-certificates curl git tar xz-utils \
            binutils gnupg libcurl4-openssl-dev libc6-dev libedit2 libgcc-13-dev \
            libncurses-dev libpython3-dev libstdc++-13-dev libxml2-dev libz3-dev \
            pkg-config tzdata unzip zlib1g-dev

      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1

      - name: Install component
        shell: bash
        run: |
          set -euo pipefail
          bash "src/assets/setup/${{ matrix.component }}/install.sh"
