* Julesの各ワーカー（Observer/Decider/Plannerなど）は、実行結果を必ずブランチを作成して反映させなければならない。そして、GitHubへ反映する時に branch を作るだけか、PRを作成する前提。
* 実行環境はセキュリティ制限があり、自分の作業ブランチ以外の`git push` を含むコマンドがブロックされる（= リモートブランチ削除や任意pushでの直接更新はできない）。
