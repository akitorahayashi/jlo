# jlo agent configuration
# Configure which roles to run for each layer

[agents]
observers = ["taxonomy", "data_arch", "qa", "consistency"]
deciders = ["triage"]
planners = ["specifier"]
implementers = ["executor"]

[run]
default_branch = "main"
parallel = true
max_parallel = 3

[jules]
# Jules API endpoint
# api_url = "https://api.jules.ai/v1/sessions"
# Request timeout in seconds
# timeout_secs = 30
# Maximum retry attempts on transient failures
# max_retries = 3
