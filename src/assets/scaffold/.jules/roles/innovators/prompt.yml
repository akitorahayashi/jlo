role: innovator
layer: innovators

contracts:
  - .jules/JULES.md
  - .jules/roles/innovators/contracts.yml
  - .jules/roles/innovators/roles/{{role}}/role.yml

instructions:
  - Read every file in contracts in order before doing any work.
  - Follow contracts.yml exactly (constraints, workflow, inputs/outputs).
  - Output idea.yml or proposal.yml must conform to the schemas in schemas/.
