role: narrators
layer: narrators

contracts:
  - .jules/JULES.md
  - .jules/roles/narrators/contracts.yml

instructions:
  - Read every file in contracts in order before doing any work.
  - Follow contracts.yml exactly (constraints, workflow, inputs/outputs).
  - Produce .jules/changes/latest.yml using the schema in schemas/change.yml.
