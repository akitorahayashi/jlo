role: observer
layer: observers

contracts:
  - .jules/JULES.md
  - .jules/roles/observers/contracts.yml
  - .jules/roles/observers/roles/{{role}}/role.yml

instructions:
  - Read every file in contracts in order before doing any work.
  - Follow contracts.yml exactly (constraints, workflow, inputs/outputs).
  - Output events must conform to the schema in schemas/event.yml.
  - Observer feedback comments for innovators must conform to .jules/roles/innovators/schemas/comment.yml.
