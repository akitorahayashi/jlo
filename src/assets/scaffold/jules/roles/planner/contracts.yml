layer: planner
branch_prefix: jules-planner-

purpose:
  Expand requirements that require deep analysis with full system impact assessment,
  dependency analysis, risks, and implementation specifics.

constraints:
  - "Branch names must start with the prefix jules-planner-"
  - "Do not modify files outside of the .jules/ directory."
