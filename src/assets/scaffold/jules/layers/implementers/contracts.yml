layer: implementers
branch_prefix: jules-implementer-

purpose:
  Implement the assigned issue by modifying source code, tests, and documentation,
  then create a pull request with the changes.

constraints:
  - "Branch format: jules-implementer-<label>-<id>-<short_description>"
  - "<label> must match the issue label"
