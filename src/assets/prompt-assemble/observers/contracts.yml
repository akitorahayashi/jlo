layer: observers
branch_prefix: jules-observer-

purpose:
  Inspect the codebase through the lens of your specialized role (role.yml) and
  emit 0-3 high-signal, evidence-backed findings as events.

constraints:
  - "Do not duplicate findings already covered by open requirements or recent events"
  - "Branch names must start with the prefix jules-observer-"
  - "Do not modify files outside of the .jules/ directory."
  - "Respect role.yml constraints and evidence expectations"
