schema_version: 2

# Metadata
id: "doc2s1"
source_events:
  - "a1b2c3"
title: "Document Update Flag"
label: "docs"
priority: "low"
summary: "Document --adopt-managed flag in AGENTS.md."

# Content
problem: |
  The `AGENTS.md` file lists the `jlo update` command but omits the `--adopt-managed` flag, which is present in the implementation and documented in `README.md`.

impact: |
  Agents relying on `AGENTS.md` are unaware of this capability, potentially leading to suboptimal workflows when updating the workspace.

desired_outcome: |
  `AGENTS.md` accurately reflects the available CLI options for `jlo update`.

# Specifics
affected_areas:
  - "AGENTS.md"

acceptance_criteria:
  - "`AGENTS.md` documents `jlo update --adopt-managed`."

verification_commands:
  - "grep 'adopt-managed' AGENTS.md"

# Analysis Flags
requires_deep_analysis: false
deep_analysis_reason: ""
