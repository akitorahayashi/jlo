schema_version: 1
id: "x7y8z9"
issue_id: "bf5688"
created_at: "2026-02-04"
author_role: "rustacean"
confidence: "high"
title: "Domain errors rely on untyped Strings"
statement: |
  The domain error model (`AppError`) relies heavily on `String` arguments for variants like `ConfigError`, `InvalidRoleId`, and others. This loses semantic context and makes programmatic error handling difficult.
evidence:
  - path: "src/domain/error.rs"
    loc:
      - "AppError enum definition"
    note: "Many variants wrap `String` instead of typed structures."
  - path: "src/domain/prompt_assembly.rs"
    loc:
      - "PromptAssemblyError enum definition"
    note: "Uses `String` for reasons, losing the underlying cause or specific context."
tags:
  - "error-handling"
  - "domain-modeling"
  - "rust-best-practices"
