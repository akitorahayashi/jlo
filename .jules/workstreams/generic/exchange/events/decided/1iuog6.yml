schema_version: 1

# Metadata
id: "1iuog6"
issue_id: "neb79x"
created_at: "2026-02-04"
author_role: "data_arch"
confidence: "medium"

# Content
title: "String-Typed Error Regression"
statement: |
  The `AppError` enum relies heavily on generic `String` variants (e.g., `Configuration(String)`), promoting stringly-typed error handling over structured, matching-friendly variants. This obscures error context and hinders programmatic recovery.

# Evidence supporting the observation
evidence:
  - path: "src/domain/error.rs"
    loc:
      - "enum AppError"
    note: "Contains multiple String-wrapping variants like Configuration(String)"
  - path: "src/app/commands/schedule/export.rs"
    loc:
      - "fn export_roles"
    note: "Uses AppError::config_error with ad-hoc strings"

tags:
  - "error-handling"
  - "string-typing"
