schema_version: 1
id: "z9x8c7"
issue_id: "neb79x"
created_at: "2026-02-04"
author_role: "rustacean"
confidence: "high"
title: "AppError wraps PromptAssemblyError as String"
statement: |
  The `AppError` enum wraps `PromptAssemblyError` using a `String` variant, causing loss of semantic information and context. `PromptAssemblyError` is a fully defined enum in the domain layer and should be wrapped transparently or structurally.
evidence:
  - path: src/domain/error.rs
    loc:
      - "PromptAssemblyError(String)"
    note: "Wraps typed error as String"
  - path: src/domain/prompt_assembly.rs
    loc:
      - "pub enum PromptAssemblyError"
    note: "The original typed error definition"
tags:
  - "error-modeling"
  - "types"
