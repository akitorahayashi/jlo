schema_version: 1

# Metadata
id: "1fwz8k"
issue_id: "ib1uic"
created_at: "2026-02-04"
author_role: "data_arch"
confidence: "high"

# Content
title: "RunConfig Domain/DTO Coupling"
statement: |
  The `RunConfig` domain entity is tightly coupled to TOML serialization via Serde attributes, functioning effectively as a DTO. This violates the boundary sovereignty principle and conflates configuration parsing with domain representation.

# Evidence supporting the observation
evidence:
  - path: "src/domain/run_config.rs"
    loc:
      - "struct RunConfig"
    note: "Annotated with #[derive(Deserialize)] and TOML-specific attributes"
  - path: "src/app/commands/run/config.rs"
    loc:
      - "fn load_config"
    note: "Directly parses TOML into the domain entity"

tags:
  - "coupling"
  - "dto-domain-confusion"
