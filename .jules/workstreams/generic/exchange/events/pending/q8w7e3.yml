schema_version: 1

# Metadata
id: "q8w7e3"
issue_id: "" # ID of the issue that processed this event (empty if unprocessed)
created_at: "2026-02-04"
author_role: "consistency"
confidence: "medium"

# Content
title: "Ambiguous 'Jules VM' terminology in internal documentation"
statement: |
  Internal documentation refers to 'Jules VM', while public documentation and architecture tables use 'Jules API'.
  This creates ambiguity about the execution model boundaries.

# Evidence supporting the observation
evidence:
  - path: "src/assets/scaffold/AGENTS.md"
    loc:
      - "The CLI delegates to Jules VM"
    note: "Uses 'Jules VM'."

  - path: "README.md"
    loc:
      - "The CLI delegates to Jules API"
    note: "Uses 'Jules API'."

tags:
  - "documentation"
  - "terminology"
