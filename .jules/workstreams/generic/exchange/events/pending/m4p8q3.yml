schema_version: 1

# Metadata
id: "m4p8q3"
issue_id: ""
created_at: "2026-02-04"
author_role: "taxonomy"
confidence: "high"

# Content
title: "Inconsistent error naming conventions"
statement: |
  The AppError enum exhibits inconsistent naming conventions, mixing 'NounCondition' (WorkspaceNotFound), 'ConditionNoun' (InvalidRoleId), and 'SourceError' (GitError) patterns. Additionally, the PromptAssemblyError variant creates a name collision with the PromptAssemblyError domain type.

# Evidence supporting the observation
evidence:
  - path: "src/domain/error.rs"
    loc:
      - "enum AppError"
    note: "Mixes naming patterns like WorkspaceNotFound (Noun+Condition), InvalidRoleId (Condition+Noun), and GitError (Source+Error)"

  - path: "src/domain/prompt_assembly.rs"
    loc:
      - "enum PromptAssemblyError"
    note: "Defines a domain error type that shares the same name as the AppError variant"

tags:
  - "taxonomy"
  - "consistency"
  - "naming"
