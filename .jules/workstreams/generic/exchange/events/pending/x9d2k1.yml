schema_version: 1

# Metadata
id: "x9d2k1"
issue_id: ""
created_at: "2026-02-04"
author_role: "taxonomy"
confidence: "high"

# Content
title: "Duplicate identifier validation boilerplate"
statement: |
  RoleId and ComponentId implement nearly identical validation wrapper logic (newtype pattern with string validation and delegating to validation::validate_identifier), creating unnecessary boilerplate and violating DRY.

# Evidence supporting the observation
evidence:
  - path: "src/domain/role_id.rs"
    loc:
      - "struct RoleId"
      - "impl RoleId"
      - "validate_identifier"
    note: "Implements newtype wrapper with validation logic almost identical to ComponentId"

  - path: "src/domain/component_id.rs"
    loc:
      - "struct ComponentId"
      - "impl ComponentId"
      - "validate_identifier"
    note: "Implements newtype wrapper with validation logic almost identical to RoleId"

tags:
  - "taxonomy"
  - "refactor"
  - "dry"
