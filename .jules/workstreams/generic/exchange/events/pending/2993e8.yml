schema_version: 1
id: "2993e8"
issue_id: ""
created_at: "2026-02-06"
author_role: "data_arch"
confidence: "high"
title: "Fragile untyped YAML parsing in matrix routing"
statement: |
  The `matrix/routing` command parses YAML into `serde_yaml::Value` and accesses fields via string keys, missing the safety of strongly typed deserialization.
evidence:
  - path: "src/app/commands/workflow/matrix/routing.rs"
    loc:
      - "let value: serde_yaml::Value ="
      - "match &value[\"requires_deep_analysis\"]"
    note: "Parses YAML to Value and uses string indexing instead of defining a struct."
tags:
  - "fragile-parsing"
  - "type-safety"
