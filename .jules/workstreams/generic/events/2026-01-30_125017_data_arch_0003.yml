schema_version: 1

# Metadata
id: "2026-01-30_125017_data_arch_0003"
created_at: "2026-01-30"
author_role: "data_arch"
confidence: "high"

# Content
title: "Primitive Obsession in RunConfig and Setup"
statement: |
  `RunConfig` uses `String` for `api_url` instead of a typed URL value object. `Component` uses `String` for identifiers. This reduces type safety and expressiveness.

evidence:
  - path: "src/domain/run_config.rs"
    loc: "38"
    note: "api_url is String"
  - path: "src/domain/setup.rs"
    loc: "20"
    note: "Component name is String"

tags:
  - "primitive_obsession"
  - "weak_typing"
