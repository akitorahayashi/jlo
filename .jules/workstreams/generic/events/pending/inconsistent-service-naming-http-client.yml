schema_version: 1

# Metadata
id: "x7k9p2"
issue_id: ""
created_at: "2026-02-01"
author_role: "taxonomy"
confidence: "high"

# Content
title: "Inconsistent Service Naming: HttpJulesClient"
statement: |
  The service `HttpJulesClient` is defined in `jules_client_http.rs`, violating the `Type` = `Filename` convention (which implies `HttpJulesClient` -> `http_jules_client.rs` or `jules_client_http.rs` -> `JulesClientHttp`) and exhibiting inconsistent ordering (Prefix `Http` vs Suffix `_http`).

# Evidence supporting the observation
evidence:
  - path: "src/services/jules_client_http.rs"
    loc:
      - "struct HttpJulesClient"
    note: "Struct name uses Prefix 'Http', filename uses Suffix '_http'."

tags:
  - "naming-convention"
  - "consistency"
