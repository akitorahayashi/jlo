schema_version: 1
id: cnm001
created_at: "2024-05-20"
author_role: taxonomy
confidence: high
title: "Config Name Mismatch: SetupConfig vs tools.yml"
statement: |
  There is a direct naming mismatch between the `SetupConfig` struct and the configuration file it represents (`tools.yml`). This violates the "One Concept, One Preferred Term" principle.
evidence:
  - path: "src/domain/setup.rs"
    loc:
      - "pub struct SetupConfig { pub tools: Vec<String> }"
    note: "Struct named 'SetupConfig' implies generic setup configuration."
  - path: "src/app/commands/setup/generate.rs"
    loc:
      - "let tools_yml = setup_dir.join(\"tools.yml\");"
      - "let config: SetupConfig = serde_yaml::from_str..."
    note: "Code maps 'tools.yml' directly to 'SetupConfig'."
tags:
  - taxonomy
  - configuration
  - naming-consistency
