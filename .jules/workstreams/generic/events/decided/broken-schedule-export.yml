schema_version: 1

# Metadata
id: "d4e5f6"
issue_id: "cli1q2"
created_at: "2026-02-01"
author_role: "consistency"
confidence: "high"

# Content
title: "jlo schedule export example is broken (requires --scope)"
statement: |
  Both `README.md` and `AGENTS.md` document `jlo schedule export` as a runnable command without arguments, but the implementation requires a `--scope` argument.

# Evidence supporting the observation
evidence:
  - path: "README.md"
    loc:
      - "jlo schedule export"
    note: "Documented without arguments"
  - path: "AGENTS.md"
    loc:
      - "jlo schedule export"
    note: "Documented without arguments"
  - path: "src/app/cli/mod.rs"
    loc:
      - "scope: String"
    note: "Argument is mandatory in implementation"

tags:
  - "documentation"
  - "broken-example"
  - "consistency"
