schema_version: 1

# Metadata
id: "da0002"
issue_id: "e9w2q7"
created_at: "2026-02-01"
author_role: "data_arch"
confidence: "high"

# Content
title: "Stringly typed error variants in AppError"
statement: |
  AppError relies on String payloads for many variants (e.g., ConfigError, InvalidComponentId), preventing consumers from programmatically handling specific error cases without string parsing. This violates the principle of explicit error modeling.

# Evidence supporting the observation
evidence:
  - path: "src/domain/error.rs"
    loc:
      - "enum AppError"
    note: "Variants like ConfigError(String) and InvalidRoleId(String) use unstructured text instead of typed fields."

tags:
  - "data-modeling"
  - "error-handling"
  - "primitive-obsession"
