schema_version: 1

# Metadata
id: "2026-01-30_125017_data_arch_0001"
created_at: "2026-01-30"
author_role: "data_arch"
confidence: "high"

# Content
title: "Redundant Component and ComponentMeta definitions"
statement: |
  The domain entity `Component` and the DTO `ComponentMeta` in `src/domain/setup.rs` define nearly identical fields, violating Normalized Independence. `ComponentMeta` should be a DTO in the adapter layer, not in the domain.

evidence:
  - path: "src/domain/setup.rs"
    loc: "18-30"
    note: "Component struct definition"
  - path: "src/domain/setup.rs"
    loc: "41-52"
    note: "ComponentMeta struct definition"

tags:
  - "redundant_definition"
  - "normalized_independence"
