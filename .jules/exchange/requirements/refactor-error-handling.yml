schema_version: 2

# Metadata
id: "errdec"
source_events:
  - "e4r9x2"
title: "Decompose Monolithic AppError"
label: "refacts"
priority: "medium"
summary: "Split the monolithic `AppError` enum into domain-specific error types."

# Content
goal: |
  Improve modularity and error specificity.

problem: |
  `AppError` is a God Object covering IO, Git, API, Setup, etc.

impact: |
  Tight coupling. Unclear error boundaries.

desired_outcome: |
  Domain-specific errors (e.g., `GitError`, `ConfigError`) are defined and used.

constraints: []
risks: []

# Specifics
affected_areas:
  - "src/domain/error.rs"

acceptance_criteria:
  - "`AppError` is decomposed or significantly reduced."

verification_criteria:
  - "Review `src/domain/error.rs`."

# Analysis Flags
requires_deep_analysis: false
deep_analysis_reason: ""
