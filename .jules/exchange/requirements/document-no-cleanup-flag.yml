schema_version: 2

# Metadata
id: "docncl"
source_events:
  - "evt3b4"
title: "Document --no-cleanup Flag"
label: "docs"
priority: "low"
summary: "Add documentation for the --no-cleanup flag in README.md."

# Content
goal: |
  Ensure all CLI flags are documented in the README.

problem: |
  The `jlo run implementer` command supports a `--no-cleanup` flag, but it is not listed in the "Run Command" section of `README.md`.

impact: |
  Users are unaware of useful debugging/maintenance functionality.

desired_outcome: |
  `README.md` includes `--no-cleanup` in the flags list.

constraints: []
risks: []

# Specifics
affected_areas:
  - "README.md"

acceptance_criteria:
  - "`README.md` documents the `--no-cleanup` flag."

verification_criteria:
  - "Manual check of `README.md`."

# Analysis Flags
requires_deep_analysis: false
deep_analysis_reason: ""
