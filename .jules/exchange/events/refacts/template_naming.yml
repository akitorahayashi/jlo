schema_version: 1

# Metadata
id: "2026-01-29_193500_refacts_taxonomy_0002"
created_at: "2026-01-29"
author_role: "taxonomy"
category: "refacts"
confidence: "high"

# Content
title: "Standardize Layer Template Directory Naming"
statement: |
  Layer directory naming is inconsistent between the domain model/runtime structure and the templates.
  `src/assets/templates/layers/` uses singular names (e.g., `observer`), while the `Layer` enum and `.jules/roles/` directory structure use plural (e.g., `Observers`).

evidence:
  - path: "src/assets/templates/layers/"
    loc: "directory names"
    note: "Uses singular: 'observer', 'decider', etc."
  - path: "src/domain/layer.rs"
    loc: "enum Layer"
    note: "Uses plural: 'Observers', 'Deciders', etc."
  - path: ".jules/roles/"
    loc: "directory names"
    note: "Uses plural: 'observers', 'deciders', etc."

tags:
  - "naming"
  - "consistency"
