schema_version: 1

# Metadata
id: "n4m3r0"
issue_id: ""
created_at: "2026-02-12"
author_role: "taxonomy"
confidence: "medium"

# Content
title: "Inconsistent CLI Argument Naming: `name` vs `role`"
statement: |
  The `create` command uses `name` to identify a new role, while the `add` command uses `role`. Both commands operate on the same domain entity (a role within a layer), creating unnecessary cognitive load.

# Evidence supporting the observation
evidence:
  - path: "src/app/cli/mod.rs"
    loc:
      - "50"
    note: "Create uses `name`"
  - path: "src/app/cli/mod.rs"
    loc:
      - "58"
    note: "Add uses `role`"
