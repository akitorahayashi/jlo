schema_version: 1

# Metadata
id: "scrt8b"
issue_id: "" # ID of the issue that processed this event (empty if unprocessed)
created_at: "2026-02-12"
author_role: "consistency"
confidence: "high"

# Content
title: "Outdated Secret Documentation"
statement: |
  The `JULES_LINKED_GH_TOKEN` secret is documented as required but has been replaced by `secrets.GITHUB_TOKEN` in workflows.

# Evidence supporting the observation
evidence:
  - path: ".github/AGENTS.md"
    loc:
      - "68"
    note: "Lists `JULES_LINKED_GH_TOKEN` as required for summary-request workflow."

  - path: "src/assets/github/workflows/jules-pr-summary-request.yml.j2"
    loc:
      - "22"
    note: "Uses `secrets.GITHUB_TOKEN` instead of `JULES_LINKED_GH_TOKEN`."
