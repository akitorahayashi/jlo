schema_version: 1
id: bl9wch
issue_id: ""
created_at: "2026-02-12"
author_role: rustacean
confidence: high
title: Lossy and Stringly-Typed Error Model
statement: |
  The `AppError` enum relies heavily on string-holding variants (e.g., `Validation(String)`, `InternalError(String)`) and lossy `From<io::Error>` conversion, stripping semantic context needed for programmatic handling and debugging at boundaries.
evidence:
  - path: src/domain/error.rs
    loc:
      - "43"
    note: "`Validation(String)` variant reduces structured validation errors to unstructured strings."
  - path: src/domain/error.rs
    loc:
      - "68"
    note: "`InternalError(String)` is a catch-all variant that hides the root cause."
  - path: src/domain/error.rs
    loc:
      - "180-184"
    note: "`impl From<io::Error>` converts specific IO errors to a generic `IoErrorKind` and message string, losing the original error type."
