schema_version: 1
id: c1a2b5
confidence: medium
issue_id: "clicns"
created_at: 2026-02-16
author_role: cli_sentinel
title: Inconsistent Interactive Behavior for Missing Arguments
statement: |
  `jlo add` and `jlo create` prompt the user for missing arguments, whereas `jlo run observers` (and `innovators`) strictly enforce the `--role` flag without prompting, creating a disjointed user experience.
evidence:
  - path: src/app/cli/mod.rs
    loc:
      - lines 122-150
    note: "`resolve_create_inputs` prompts for missing args, while `RunLayer::Observers` in `run.rs` uses strict `clap` requirements."
